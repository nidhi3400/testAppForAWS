/** Configures the auth modal appearance. */
export interface AuthModalOptions {
    /** An optional text to use for the modal title. Useful for i18n. */
    title?: string;
    /** An optional text to use for the modal body. Useful for i18n. */
    body?: string;
    /** An optional text to use for the button. Useful for i18n. */
    buttonText?: string;
}
export interface CreateAuthModalOptions extends AuthModalOptions {
    /** A callback that must be invoked whenever the authentication window should be opened. */
    openAuthWindow(): void;
}
export interface AuthModal {
    /** A callback that will be invoked whenever the modal is closed. */
    close(): void;
}
/** A callback that creates an auth modal. */
export declare type CreateAuthModalCallback = (options: CreateAuthModalOptions) => AuthModal;
/** Function used for unit testing purposes. */
export declare function clearAuthWindowUrl(): void;
/**
 * Shows a modal asking users to authenticate with Midway using a popup.
 * Returns a `Promise` that resolves when the user has authenticated successfully with the auth token.
 * @param url The authentication URL to show.
 * @param fetchToken A callback that returns a `Promise` with the Midway token.
 * @param options The modal options.
 * @param createAuthModal A callback for creating a custom auth modal.
 */
export declare function showAuthModal(url: string, fetchToken: () => Promise<string>, options?: AuthModalOptions, createAuthModal?: CreateAuthModalCallback): Promise<string>;
