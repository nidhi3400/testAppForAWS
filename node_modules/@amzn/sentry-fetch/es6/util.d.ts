/** A clock skew to use for considering a token valid. */
export declare const CLOCK_SKEW: number;
/**
 * Sentry sso services have a /sso/login endpoint that returns returns a authn_endpoint.
 * SentryFetch reads a request url to figure the endpoint to call /sso/login, ie https://some-domain/foo/bar to https://some-domain + /sso/login
 * This utility method attempts to extract the endpoint from a URL.
 */
export declare type GetEndpointOverride = (request: RequestInfo | URL) => string;
export declare function getEndpoint(request: Request, override?: GetEndpointOverride): string;
/**
 * Obtains the session storage key used to cache authentication token expiry times.
 * @param endpoint The endpoint value.
 */
export declare function getSessionStorageKey(endpoint: string): string;
